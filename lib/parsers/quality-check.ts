// TODO: Parse output validation — confidence check, section detection
export function validateParseQuality(result: unknown): { valid: boolean; confidence: number } {
  return { valid: true, confidence: 0 };
}
